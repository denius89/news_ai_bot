tunnel: scoring-side-receives-hudson
credentials-file: /Users/denisfedko/.cloudflared/scoring-side-receives-hudson.json

ingress:
  - hostname: scoring-side-receives-hudson.trycloudflare.com
    service: http://localhost:8001
    originRequest:
      # Разрешаем все заголовки для Telegram WebApp
      noTLSVerify: true
      # Передаем все заголовки
      httpHostHeader: scoring-side-receives-hudson.trycloudflare.com
      # Разрешаем CORS
      originServerName: localhost:8001
  - service: http_status:404
