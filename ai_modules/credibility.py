import random


def evaluate_credibility(news_item: dict) -> float:
    """
    Заглушка: возвращает случайное значение достоверности [0, 1].
    В будущем заменим на вызов AI-модели.
    """
    return round(random.uniform(0.5, 1.0), 2)
