# 📝 TASKS (last updated: 2025-09-24 12:26:49)

## Сегодня — Day 01 Sprint Board
- [ ] Создать/обновить docs: VISION.md, ROADMAP.md, COMMUNICATION.md (🟡)
- [ ] Обновить MASTER_FILE.md (ссылки на новые docs + правила/решения) (🟡)
- [ ] Обновить TASKS.md (дни 1–5 из Roadmap) (🟡)
- [ ] Ревью rss_parser.py и events_parser.py (🔴)
- [ ] Проверка записи в db_models.py + структурированное логирование (🔴)
- [ ] Тестовый прогон: загрузить источники → проверить БД (🔴)

---

## Мини-саммари (вечер)
**✅ Сделано:**  
— Созданы и заполнены VISION.md, ROADMAP.md, COMMUNICATION.md  
— Обновлён MASTER_FILE.md (ссылки, правила, решения)  
— Обновлён TASKS.md (Day 1–5)  
— Ревью и рефакторинг rss_parser.py и events_parser.py  
— Рефакторинг db_models.py (uid, upsert, логирование)  
— Тестовый прогон: данные загружены в Supabase, дубли не вставляются  

**🔜 В работе:**  
— Поддержка таблицы events (upsert_events, расширение парсинга)  
— Расширение CI workflow  

**🚧 Блокеры:**  
— DNS-проблема с feeds.reuters.com (временно убран из sources.yaml)  
— У Axios нет рабочего RSS (удалён из sources.yaml)

---

## Дополнительные задачи (Day 01)

### Тесты для парсеров
**Приоритет:** 🟡  
**Статус:** ✅ Выполнено 2025-09-24  
**Контекст (зачем):**  
Убедиться, что rss_parser и events_parser корректно работают, даты нормализуются в UTC, uid/event_id уникальны.  

**Что сделано:**
- Добавлен файл `tests/test_parsers.py`.  
- Покрыты функции `clean_text`, `normalize_date`, `normalize_datetime`, `make_event_id`.  
- Проверена уникальность `uid` и `event_id`.  
- Тесты проходят успешно (`pytest -q` все зелёные).  

**Результат:**  
- Парсеры стабильно возвращают валидные данные.  
- Дубликаты не появляются.

---

### Поддержка таблицы events в БД
**Приоритет:** 🟡  
**Статус:** ✅ Выполнено 2025-09-24  
**Контекст (зачем):**  
Сохранять события (macro) в Supabase для календаря.  

**Что сделано:**
- Добавлена колонка `event_id` + индекс в таблице events.  
- Реализована функция `upsert_event()` в db_models.py (алиас `upsert_events`).  
- Создан скрипт `tools/fetch_and_store_events.py` для загрузки и вставки событий.  
- Протестировано: данные вставляются, дубликаты не появляются.  

**Результат:**  
- В Supabase 36 уникальных событий.  
- Повторные запуски не создают дублей.  
---

### Расширение CI workflow
**Приоритет:** 🟢  
**Статус:** ✅ Выполнено 2025-09-24  
**Контекст (зачем):**  
Чтобы тесты и стиль проверялись автоматически при каждом PR.  

**Что сделано:**
- Добавлен `.github/workflows/tests.yml` с шагами: flake8, black --check, pytest с coverage ≥ 60%.  
- Используется `pytest.ini` с маркером `integration` (исключены интеграционные тесты по умолчанию).  
- Локально пройдено автоформатирование (`black .`), исправлены ошибки линтера (`flake8`).  
- Все тесты проходят зелёным, CI на GitHub Actions работает стабильно.  

**Результат:**  
- На PR запускается job `tests (py3.11)`.  
- При падении линтера/тестов merge блокируется.  
- Интеграционные тесты можно запускать вручную.

---

### Авто-саммари по CI
**Приоритет:** 🟢  
**Статус:** ✅ Выполнено 2025-09-24  
**Контекст (зачем):**  
Быстро видеть статус сборок и прогонов.  

**Что сделано:**
- В `README.md` добавлен бейдж статуса CI (GitHub Actions → Tests workflow).  
- CI проходит зелёным, бейдж обновляется автоматически.  
- Опциональные уведомления в Telegram пока не подключали.  

**Результат:**  
- В README всегда видно актуальное состояние CI.  
- При падении пайплайна бейдж меняет цвет на красный.

## История изменений
- ✅ 2025-09-23 — Монетизация зафиксирована в README.md 

## Легенда приоритетов
- 🔴 Срочно (блокирует работу)  
- 🟡 Важно (желательно сделать в ближайшие дни)  
- 🟢 Можно отложить  

## Шаблон задачи

### [Заголовок задачи]
**Приоритет:** 🔴/🟡/🟢  
**Контекст (зачем):**  
Краткое объяснение, зачем нужна задача.  

**Подзадачи:**
- [ ] шаг 1
- [ ] шаг 2

**Критерии приёмки:**
- Условие 1
- Условие 2

**Связанные решения:** (ссылка на Историю решений в MASTER_FILE.md)

---

## Основные задачи (backlog)

### Добавить фильтр по темам
**Приоритет:** 🟡  
**Контекст (зачем):**  
Чтобы пользователи могли формировать персональные дайджесты.  

**Подзадачи:**
- [ ] Расширить модель в БД (таблица тем)  
- [ ] Добавить выбор тем в config/sources.yaml  
- [ ] Добавить фильтр в webapp.py  

**Критерии приёмки:**
- В webapp можно выбрать тему  
- В дайджест попадают только новости по теме  

**Связанные решения:** см. MASTER_FILE.md от 2025-09-23  

---

### Ревизия документации
**Приоритет:** 🟡  
**Контекст (зачем):**  
Документация должна быть актуальной для разработчиков и пользователей.  

**Подзадачи:**
- [ ] Обновить MASTER_FILE.md (структура общения, история решений)  
- [ ] Обновить README.md (Quickstart, структура, правила)  
- [ ] Привести TASKS.md в единый формат  

**Критерии приёмки:**
- Все три файла согласованы между собой  
- Нет устаревших ссылок или несогласованностей  

**Связанные решения:** см. MASTER_FILE.md от 2025-09-23  

---

### Расширение тестов
**Приоритет:** 🟡  
**Контекст (зачем):**  
Повысить стабильность ETL и AI-модулей.  

**Подзадачи:**
- [ ] Тесты для ETL: парсинг RSS (моки/фикстуры), нормализация дат, upsert в БД без дублей
- [ ] Тесты для AI-модулей: `importance`, `credibility` (детерминированные входы → проверяем тип/диапазон выходов)
- [ ] Маркировка интеграционных тестов и исключение их из CI по умолчанию

**Критерии приёмки:**
- `pytest` проходит локально без падений
- Покрытие ключевых модулей выросло (минимум: ETL и AI-модули задеты тестами)
- Интеграционные тесты можно запустить отдельной меткой 

---

### Доработка ETL-пайплайна
**Приоритет:** 🟡  
**Контекст (зачем):**  
Очистка данных, защита от пустых новостей, идемпотентность при повторных запусках.  

**Подзадачи:**
- [ ] Очистка HTML-тегов из текста (разрешить базовые теги или полностью очищать до plain text)
- [ ] Fallback для пустых `content` → брать `summary` → затем `title`
- [ ] Дедупликация: `uid = sha256(url|title)`; upsert по `uid`
- [ ] Логирование ошибок и пропусков (source_id, url, причина)
- [ ] Валидация записи перед БД: обязательные поля (`source_id`, `url`, `title`)
- [ ] Нормализация дат в UTC, сохранение сырой даты отдельно (если есть)

**Критерии приёмки:**
- В базе **нет** записей с пустым `content` и `title`
- Повторный прогон **не создаёт** дублей (проверка по `uid`)
- Логи содержат причинно-полезные сообщения (ERROR/WARNING) по проблемным элементам

---

### Логирование и конфиг
**Приоритет:** 🟡  
**Контекст (зачем):**  
Единый формат логов для парсеров/БД и снижение шума.  

**Подзадачи:**
- [ ] Добавить `config/logging.yaml` (консольный хендлер, уровни, именованные логгеры parsers.rss / parsers.events / database)
- [ ] Инициализация логов в точке входа (fallback на `basicConfig`)

**Критерии приёмки:**
- Логи единообразные (formatter), без дублирования и лишнего шума
- Можно поднять/понизить уровень логов без правки кода

---

### Поддержка календаря событий
**Приоритет:** 🟡  
**Контекст (зачем):**  
События должны иметь приоритеты для аналитики и отображения.  

**Подзадачи:**
- [ ] Расширить модель `events` в базе (поле `priority` + индексы по дате/приоритету)
- [ ] Парсить приоритеты с источника (и/или маппинг low/medium/high → 1/2/3)
- [ ] Отображать бейдж приоритета в webapp

**Критерии приёмки:**
- У событий в БД есть `priority` (валидные значения)
- В webapp отображается бейдж приоритета и сортировка/фильтр по нему 

---

### Добавление новых источников
**Приоритет:** 🟡  
**Контекст (зачем):**  
Расширить охват новостей (crypto + economy).  

**Подзадачи:**
- [ ] Обновить config/sources.yaml  
- [ ] Протестировать парсинг новых RSS  
- [ ] Добавить в webapp список новых категорий
- [ ] Проверка схемы `config/sources.yaml` (валидация полей: id, type, url, enabled, tags)  

**Критерии приёмки:**
- Новости из новых источников появляются в БД  
- В webapp можно выбрать новые категории  

---

### Telegram-бот для уведомлений
**Приоритет:** 🟢  
**Контекст (зачем):**  
Доставлять дайджесты и события напрямую в Telegram.  

**Подзадачи:**
- [ ] Написать бота на aiogram/pyTelegramBotAPI  
- [ ] Привязать к БД (получение дайджестов)  
- [ ] Тестовые рассылки  

**Критерии приёмки:**
- Бот отвечает на команды  
- Бот умеет отправлять дайджест  

---

### White-label интеграции для медиа
**Приоритет:** 🟢  
**Контекст (зачем):**  
Дать медиа готовое решение для встраивания дайджестов.  

**Подзадачи:**
- [ ] Определить формат API/экспорта  
- [ ] Сделать шаблон white-label фронтенда  

**Критерии приёмки:**
- Есть API-эндпоинт для выдачи дайджеста  
- White-label шаблон можно кастомизировать  

---

### Автоматические дайджесты
**Приоритет:** 🟢  
**Контекст (зачем):**  
Дайджесты должны приходить автоматически утром/вечером.  

**Подзадачи:**
- [ ] Настроить крон (утро и вечер)  
- [ ] Добавить опцию авто-генерации в webapp  

**Критерии приёмки:**
- Автодайджесты формируются в логах  
- Можно включать/выключать автогенерацию