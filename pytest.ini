# pytest.ini
[pytest]
addopts = -q --disable-warnings -m "not integration"
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
    unit: marks tests as unit (deselect with '-m "not unit"')
